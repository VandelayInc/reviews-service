dependencies:
  override:
    - yarn
  pre:
    - yarn install babel-core
    - yarn install babel-loader
    - yarn install babel-preset-es2015
    - yarn install babel-preset-react
    - yarn install enzyme
    - yarn install enzyme-adapter-react-16
    - yarn install express
    - yarn install jquery
    - yarn install mongoose
    - yarn install node
    - yarn install react
    - yarn install react-dom
    - yarn install react-test-renderer
    - yarn install sinon
    - yarn install supertest
    - yarn install webpack